{
  "name": "quoting-project",
  "version": "1.0.0",
  "description": "Agentforce Intelligent Quoting - Complete Platform",
  "private": true,
  "scripts": {
    "dev": "cd packages/presentation && npm run dev",
    "build": "cd packages/presentation && npm install && npm run build",
    "start": "cd packages/presentation && npm run start:server"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
